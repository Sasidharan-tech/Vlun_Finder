<UserControl x:Class="SOCBOX.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MainBackgroundBrush}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
            <TextBox x:Name="TargetInput" Width="350" Height="38" FontSize="16" FontFamily="Consolas"
                     Background="#181818" Foreground="{DynamicResource NeonGreenBrush}" BorderBrush="{DynamicResource NeonGreenBrush}"
                     Margin="0,0,16,0" Padding="8"/>
            <Button Content="Quick Scan" Style="{StaticResource GlowButtonStyle}" Width="120" Height="38" Margin="0,0,12,0"/>
            <Button Content="Deep Scan" Style="{StaticResource GlowButtonStyle}" Width="120" Height="38"/>
        </StackPanel>
        <!-- Neon Progress Bar -->
        <ProgressBar x:Name="ScanProgressBar" Grid.Row="1" Height="10" Margin="0,0,0,24"
                     Foreground="{DynamicResource NeonGreenBrush}" Background="#222"/>
        <!-- Terminal Output Panel -->
        <Border Grid.Row="2" Background="#000" CornerRadius="10" Padding="16" Effect="{DynamicResource CardGlowEffect}">
            <StackPanel>
                <TextBlock Text="Scan Terminal Output" Foreground="{DynamicResource NeonGreenBrush}" FontFamily="Consolas" FontSize="14"/>
                <ScrollViewer Height="220" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
                    <TextBlock Text="[12:01:01] > Quick scan started...&#x0a;[12:01:02] > Target: example.com&#x0a;[12:01:03] > Scanning...&#x0a;[12:01:05] > No critical vulnerabilities found.&#x0a;[12:01:06] > Scan complete." Foreground="#00ff9f" FontFamily="Consolas" FontSize="13"/>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>