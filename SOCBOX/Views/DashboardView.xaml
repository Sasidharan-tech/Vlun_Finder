<UserControl x:Class="SOCBOX.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:SOCBOX.Views"
             Background="{DynamicResource MainBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Cards Row -->
        <UniformGrid Rows="1" Columns="4" Margin="0,0,0,24">
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="10" Margin="8" Padding="16" Effect="{DynamicResource CardGlowEffect}">
                <StackPanel>
                    <TextBlock Text="Total Scans" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                    <TextBlock Text="42" Foreground="{DynamicResource NeonGreenBrush}" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="10" Margin="8" Padding="16" Effect="{DynamicResource CardGlowEffect}">
                <StackPanel>
                    <TextBlock Text="Active Scans" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                    <TextBlock Text="2" Foreground="{DynamicResource NeonGreenBrush}" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="10" Margin="8" Padding="16" Effect="{DynamicResource CardGlowEffect}">
                <StackPanel>
                    <TextBlock Text="Vulnerabilities Found" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                    <TextBlock Text="17" Foreground="{DynamicResource DangerRedBrush}" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="10" Margin="8" Padding="16" Effect="{DynamicResource CardGlowEffect}">
                <StackPanel>
                    <TextBlock Text="Risk Level" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
                    <TextBlock Text="HIGH" Foreground="{DynamicResource DangerRedBrush}" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>
        <!-- Charts and Terminal Row -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <!-- Chart -->
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="10" Margin="8" Padding="16" Effect="{DynamicResource CardGlowEffect}">
                <StackPanel>
                    <TextBlock Text="Vulnerability Severity" Foreground="{DynamicResource NeonGreenBrush}" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                    <!-- Placeholder for LiveCharts PieChart -->
                    <lvc:PieChart/>
                </StackPanel>
            </Border>
            <!-- Terminal Panel -->
            <Border Grid.Column="1" Background="#000" CornerRadius="10" Margin="8" Padding="16" Effect="{DynamicResource CardGlowEffect}">
                <StackPanel>
                    <TextBlock Text="SOC Terminal" Foreground="{DynamicResource NeonGreenBrush}" FontFamily="Consolas" FontSize="14"/>
                    <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
                        <TextBlock Text="[12:00:01] > Scan started...&#x0a;[12:00:02] > Target: [not set]&#x0a;[12:00:03] > Scanning ports...&#x0a;[12:00:05] > Vulnerability found: CVE-2023-1234&#x0a;[12:00:06] > Scan complete." Foreground="#00ff9f" FontFamily="Consolas" FontSize="13"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>