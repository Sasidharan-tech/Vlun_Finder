<UserControl x:Class="SOCBOX.Views.VulnerabilitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MainBackgroundBrush}">
    <Grid Margin="32">
        <DataGrid x:Name="VulnDataGrid" AutoGenerateColumns="False" HeadersVisibility="Column"
                  Background="#181818" Foreground="{DynamicResource PrimaryTextBrush}"
                  RowBackground="#111" AlternatingRowBackground="#181818"
                  BorderBrush="{DynamicResource NeonGreenBrush}" BorderThickness="1"
                  SelectionMode="Single" SelectionUnit="FullRow" CanUserAddRows="False"
                  FontFamily="Consolas" FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                <DataGridTemplateColumn Header="Severity" Width="1*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="6" Padding="4,2" Margin="2"
                                    Background="{Binding Severity, Converter={StaticResource SeverityToBrushConverter}}">
                                <TextBlock Text="{Binding Severity}" FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Popup for details (to be implemented) -->
    </Grid>
</UserControl>